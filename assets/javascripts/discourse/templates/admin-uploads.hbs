<AdminUploadsManager
  @uploads={{this.uploads}}
  @loading={{this.loading}}
  @canLoadMore={{this.canLoadMore}}
  @loadMore={{this.loadMore}}
  @username={{this.username}}
  @fromDate={{this.from_date}}
  @toDate={{this.to_date}}
  @onChangeParams={{this.updateParams}}
  @onClear={{this.clearFilters}}
/>