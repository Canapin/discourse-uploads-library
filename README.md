# uploads-browser

**⚠️ 99% AI-Generated. Don't use this plugin you can't review the code yourself and decide if it's safe to use.**

Adds an admin-only `admin/uploads` route to browse and filter site-wide user uploads.